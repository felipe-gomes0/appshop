# Exemplo de variáveis de ambiente
# Copie este arquivo para `.env.local` ou configure as variáveis no ambiente de execução.
# NÃO comite chaves secretas reais neste arquivo.

# Chave secreta do Stripe (obrigatória para chamadas server-side)
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# URL base da aplicação (usada para success_url / cancel_url no checkout)
NEXT_URL=http://localhost:3000

# Opcional: chave pública do Stripe (uso no cliente, se necessário)
# NEXT_PUBLIC_STRIPE_PK=pk_test_your_public_key_here

# Opcional: ambiente e porta
# NODE_ENV=development
# PORT=3000
